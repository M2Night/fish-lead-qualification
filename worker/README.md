# worker/ — moved to agent-demo-core

The lead-qualification voice worker now lives in the **agent-demo-core** monorepo at
`agents/lead_qual/` (a uv workspace member, deployed to LiveKit Cloud from that repo).
Do not deploy from here. See `../DEPLOY.md` §B and `agent-demo-core/agents/lead_qual/README.md`.
